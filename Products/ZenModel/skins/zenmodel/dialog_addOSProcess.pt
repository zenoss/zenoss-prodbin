<h2>Add OSProcess</h2>
<input type="hidden" name="userCreated" value="True">
<span id="errmsg" style="color:red;"></span>
<br/>
<p style="text-align:right;">
Process Class:&nbsp;<select name="className"
tal:define="targets python:[ x.absolute_url_path() for x in here.os.getSubOSProcessClassesGen()]">
    <option tal:repeat="target targets"
        tal:attributes="value target"
        tal:content="python:target.split('/')[-1]">/</option>
</select>
</p>
<div id="dialog_buttons">
<input tal:attributes="id string:dialog_submit;
        type string:submit;
        value string:OK;
        onclick string:return $$('dialog').submit_form('${here/os/absolute_url}')"
        name="addOSProcess:method" />
<input tal:attributes="id string:dialog_cancel;
                        type string:button;
                        value string:Cancel;
                        onclick string:$$('dialog').hide()" />
</div>
