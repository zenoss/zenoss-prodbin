#!/bin/sh

. $ZENHOME/bin/zenfunctions

PRGHOME=$ZENHOME/Products/NcoProduct
PRGNAME=NcoEventPopulator.py

LOGFILE=$CFGDIR/eventpoplog.ini
CFGFILE=$CFGDIR/eventpop.conf

PIDFILE=$VARDIR/$PRGNAME.pid

stop() {
    kill -9 `cat $PIDFILE`
}

case "$1" in
  start)
        start
        ;;
  stop)
        stop
        ;;
  restart)
        restart
        ;;
  status)
        status
        ;;
  help)
        help
        ;;
  *)
        echo $"Usage: $0 {start|stop|restart|status|help}"
        exit 1
esac

exit $?
