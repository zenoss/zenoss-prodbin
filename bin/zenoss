#!/usr/bin/env bash
#
# zenmon        This shell script takes care of starting and stopping zenmon.
#               
# chkconfig: 2345 80 30
# description: Zenoss is a monitoring program written by Zenoss, Inc.


. $ZENHOME/bin/zenfunctions
ZOPECMDS="zeoctl zopectl"
ZENCMDS="zenxevent zenmodeler zenperfsnmp zenprocess zenping zensyslog zenstatus zenactions"
CMDS="$ZOPECMDS $ZENCMDS"

reverse() {
    args=
    for i
    do
       args="$i $args"
       shift
    done
    echo $args
}

FUNC=$CMD

issue() {
    for cmd in $CMDS
    do
        echo -n "Daemon: $cmd "
        if  echo $cmd | grep -q ctl; then
            sudo $ZENHOME/bin/$cmd "$FUNC"
        else
            $ZENHOME/bin/$cmd "$FUNC"
        fi
    done
}

case "$FUNC" in
  start)
        issue
        ;;
  stop)
        CMDS=`reverse $CMDS`
        issue
        ;;
  restart)
        issue
        ;;
  status)
        issue
        ;;
  *)
        echo $"Usage: $0 {start|stop|restart|status}"
        exit 1
esac
exit $?
