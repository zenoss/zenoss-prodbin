<configure xmlns="http://namespaces.zope.org/zope"
           >
    <include package=".extdirect.zope"/>
    <include package=".guid"/>

    <!-- set up our key manager -->
    <utility factory="plone.keyring.keymanager.KeyManager"
           provides="plone.keyring.interfaces.IKeyManager"
           />
</configure>
