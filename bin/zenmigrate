#! /usr/bin/env bash

. $ZENHOME/bin/zenfunctions

PRGHOME=$ZENHOME/Products/ZenModel/migrate
PRGNAME=zenmigrate.py
CFGFILE=$CFGDIR/zenmigrate.conf
PIDFILE=$VARDIR/$PRGNAME.pid

# silly to run zenmigrate as a daemon
start() {
    run "$@"
}

notdaemon "$@"
